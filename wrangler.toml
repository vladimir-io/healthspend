# Healthspend v1.0 Standard Cloudflare Pages Configuration
# Providing deterministic build and deployment targets for 10/10 clinical engineering.

name = "healthspend"
pages_build_output_dir = "web/dist"
compatibility_date = "2024-04-01"

[vars]
PLATFORM = "HEALTHSPEND_TERMINAL"
VERSION = "1.0.0"

# Ensuring the CLI understands this is a Pages project in the /web subdirectory.
